#ifndef HYBRID_MERGE_SORT
#define HYBRID_MERGE_SORT

#include "insertion_sort.h"

// Ordena a entrada utilizando o método mergesort em conjunto
// com insertionsort
void sort(char label, int* v, size_t size, size_t limit);

#endif
